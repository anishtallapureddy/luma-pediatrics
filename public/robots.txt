# ─────────────────────────────────────────────────────────────────────────────
# PRE-LAUNCH — Luma Pediatrics is not publicly launched yet.
#
# Every page is served with <meta name="robots" content="noindex, nofollow">
# (the BaseLayout default), which is the authoritative signal that keeps the
# site out of Google/Bing results. Standard crawling is intentionally left
# ALLOWED so those engines can fetch a page and SEE the noindex — a blanket
# "Disallow: /" would hide the tag and could leave bare URLs in results anyway.
# No sitemap is published while pre-launch, so nothing is advertised.
#
# AT LAUNCH: flip the BaseLayout `noindex` default back to false, re-enable the
# @astrojs/sitemap integration + restore the "Sitemap:" line, and allow the AI
# crawlers again if you want the site cited by ChatGPT/Claude/Perplexity/etc.
# ─────────────────────────────────────────────────────────────────────────────

# Standard search crawlers: allowed to crawl (so they read the noindex tag).
User-agent: *
Disallow:

# AI training / ingestion crawlers: blocked while pre-launch so the unlaunched
# site isn't scraped into models or surfaced in AI answers.
User-agent: GPTBot
Disallow: /

User-agent: ChatGPT-User
Disallow: /

User-agent: OAI-SearchBot
Disallow: /

User-agent: ClaudeBot
Disallow: /

User-agent: Claude-Web
Disallow: /

User-agent: anthropic-ai
Disallow: /

User-agent: Google-Extended
Disallow: /

User-agent: PerplexityBot
Disallow: /

User-agent: Perplexity-User
Disallow: /

User-agent: CCBot
Disallow: /

User-agent: meta-externalagent
Disallow: /

User-agent: cohere-ai
Disallow: /

# No Sitemap line while pre-launch.
